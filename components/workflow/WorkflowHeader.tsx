import SectionHeader from "@/components/shared/SectionHeader";

export default function WorkflowHeader() {
  return (
    <SectionHeader
      badge="AI Workflow"
      title="How I Build Modern Applications"
      description="I combine modern development practices with AI-assisted workflows to build fast, scalable, and user-focused web applications."
    />
  );
}