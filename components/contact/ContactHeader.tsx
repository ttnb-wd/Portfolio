import SectionHeader from "@/components/shared/SectionHeader";

export default function ContactHeader() {
  return (
    <SectionHeader
      badge="Get In Touch"
      title="Let's Build Something Together"
      description="I'm currently open to freelance projects, remote opportunities, and collaborations. If you have an idea you'd like to build, let's connect."
    />
  );
}